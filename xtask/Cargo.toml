[package]
name = "xtask"
description = "Repository maintenance tasks (credits generation)"
version.workspace = true
edition.workspace = true
publish.workspace = true
license.workspace = true

[[bin]]
name = "xtask"
path = "src/main.rs"

[dependencies]
# Only for the marker constants that delimit the generated blocks in
# CREDITS.md, so the writer and the reader cannot drift apart.
kirafrq-credits.workspace = true
