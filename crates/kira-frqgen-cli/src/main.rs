//! `kira-frqgen` binary scaffold. The CLI surface and batch semantics are
//! #12's decision; this binary only proves the crate wiring builds.

fn main() {
    eprintln!("kira-frqgen: scaffold build; the CLI surface lands with #12.");
}
